#include <stdio.h>

int main() {
	int age = 20; // Declare int variable age and assign a value
    if (age >= 18) { // Check if age is 18 or older using >= operator
        printf("Allowed\n");// If true, print Allowed
    } 
    else {
        printf("Not allowed\n");  // Otherwise, print Not allowed
    }

    return 0;

}

