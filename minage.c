#include <stdio.h>
int main() {
	int age = 20; // Declare age and assign 20
    int minAge = 18;// Declare minAge and assign 18
    printf("%d", age >= minAge); // Check if age is at least minAge
    return 0; // End the program

}

